
<!-- AndroidManifest.xml snippet -->
<!-- Add this inside the <manifest> but outside <application> if needed -->
<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>

<!-- Inside <application> add receiver if you use BOOT_COMPLETED to re-register alarms -->
<receiver android:name="com.example.solo_mission.YourBootReceiver" android:exported="true">
    <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
    </intent-filter>
</receiver>
